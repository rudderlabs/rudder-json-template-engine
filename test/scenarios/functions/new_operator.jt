const Person = {
   Student: function(name, grade) {
        this.name = name;
        this.grade = grade;
    }
}

const foo = new Person.Student('foo', 1);
const bar = new Person.Student('bar', 2);
[
    {name: foo.name, grade: foo.grade}, 
    {...bar}
]